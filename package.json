{
  "name": "github-fast-merge",
  "description": "A Chrome extension that adds a 'Fast Merge' button to GitHub pull requests, allowing users to merge PRs with a single click.",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "compile": "tsc",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "@types/chrome": "^0.1.37",
    "@types/node": "^25.5.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3",
    "vite": "^8.0.0",
    "vite-plugin-zip-pack": "^1.2.4"
  }
}
